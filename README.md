# 🌿 EcoToloka — Платформа еко-подій

Веб-додаток для перегляду та реєстрації на екологічні заходи з аналітикою даних.

## 🚀 Технологічний стек

| Технологія | Призначення |
|---|---|
| React 18 | UI-фреймворк |
| React Router v6 | Клієнтська маршрутизація |
| Redux Toolkit | Глобальне управління станом |
| Zod | Валідація форм |
| Recharts | Візуалізація аналітики |
| Vite | Збірник проєкту |

## 📁 Структура проєкту

```
src/
├── components/       # Спільні компоненти (Layout, EventCard, EventList, ToastContainer)
├── pages/            # Сторінки (Home, Register, Participants, Analytics, NotFound)
├── store/
│   ├── index.js      # configureStore
│   └── slices/
│       ├── eventsSlice.js       # Events + EntityAdapter
│       ├── participantsSlice.js # Participants + AsyncThunk
│       ├── uiSlice.js           # Тема + Toast-повідомлення
│       └── analyticsSlice.js   # Аналітика + зовнішній API
├── schemas/
│   └── registerSchema.js  # Zod-схема валідації
└── events.json            # Локальні дані подій
```

## 🔗 Маршрути

| URL | Сторінка |
|---|---|
| `/` | Список подій з пошуком та фільтрацією |
| `/register/:eventId` | Реєстрація на подію |
| `/participants/:eventId` | Список учасників |
| `/analytics` | Аналітичний дашборд |

## ⚙️ Запуск локально

```bash
# Встановити залежності
npm install

# Запустити dev-сервер
npm run dev

# Зібрати для продакшну
npm run build
```

## 🌐 Деплой на GitHub Pages

```bash
# Встановити gh-pages
npm install --save-dev gh-pages

# Додати в package.json:
# "homepage": "https://<username>.github.io/<repo-name>"
# "predeploy": "npm run build"
# "deploy": "gh-pages -d dist"

# Задеплоїти
npm run deploy
```

### Налаштування Vite для GitHub Pages

```js
// vite.config.js
export default defineConfig({
  plugins: [react()],
  base: '/<repo-name>/',
})
```

## 🌐 Деплой на Vercel

```bash
npm install -g vercel
vercel
```

## ✨ Функціональність

- **Список подій** — пошук, фільтр «цікавих», нескінченна пагінація
- **Реєстрація** — валідація через Zod (email, вік 18+, ПІБ)
- **Учасники** — пошук за ім'ям/email, skeleton-loading
- **Аналітика** — Area/Bar/Pie/Line графіки, статистичні картки
- **Імпорт** — завантаження подій із зовнішнього API
- **Теми** — light/dark режим з збереженням у localStorage
- **Toast-сповіщення** — глобальна система через Redux